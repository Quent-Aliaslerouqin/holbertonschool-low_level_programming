#include "main.h"

/**
* print_rev - r
* @s: r
* return:void
*/
void print_rev(char *s)
{
	int i, n;

	for (i = 0; s[i] != '\0'; i++;
	{
	n = i - i;
	}
	for (i = n; i >= 0 ; i--)
	{
		_putchar(s[i]);
	}
_putchar('\n');
return;
}

