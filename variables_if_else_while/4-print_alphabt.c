#include <stdio.h>
/**
* main - code to put a message
* Return: Always 0
*/
int main(void)
{
	char alphabet;
	char letter;

	for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
	{
		if (letter != 'e' && letter != 'q'; letter++)
			putchar(letter);
	}

	putchar('\n');
	return (0);
}
